# .github/workflows/ci.yml

name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches-ignore: [main]

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # Pin v4.2.2
        with:
          fetch-depth: 0

      - name: Copy sonar-project.properties to root
        run: cp devops/sonarqube/sonar-project.properties .

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@02ef91109b2d5813d8b7280b855e2d62400e2f23 # Pin v3.1.2 (Absolute SHA)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  trivy-filesystem:
    name: Trivy Filesystem Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # Pin v4.2.2

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@696b804e638b13d4c0003023058b8590673057a1 # Pin v0.28.0 (Absolute SHA)
        with:
          scan-type: fs
          scan-ref: .
          severity: HIGH,CRITICAL
          format: table
          exit-code: 0

  build-and-scan:
    name: Build & Scan — ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [auth-service, vehicle-service, plate-service, api-gateway, frontend]
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # Pin v4.2.2

      - name: Build ${{ matrix.service }} image
        run: docker build -t ${{ matrix.service }}:ci ./${{ matrix.service }}/

      - name: Trivy image scan — ${{ matrix.service }}
        uses: aquasecurity/trivy-action@696b804e638b13d4c0003023058b8590673057a1 # Pin v0.28.0 (Absolute SHA)
        with:
          image-ref: ${{ matrix.service }}:ci
          severity: HIGH,CRITICAL
          format: table
          exit-code: 0