apiVersion: v1
kind: Secret
metadata:
  name: nvr-secrets
  namespace: nvr-production
type: Opaque
stringData:
  # ⚠️ Use a strong secret in production — minimum 32 characters
  JWT_SECRET: "nvr_production_secret_key_CHANGE_THIS_NOW_32chars"
  DB_PASSWORD: "NVR@Admin1234"
